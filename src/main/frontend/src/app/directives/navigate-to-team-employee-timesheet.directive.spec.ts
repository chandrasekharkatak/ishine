import { NavigateToTeamEmployeeTimesheetDirective } from './navigate-to-team-employee-timesheet.directive';

describe('NavigateToTeamEmployeeTimesheetDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateToTeamEmployeeTimesheetDirective();
    expect(directive).toBeTruthy();
  });
});
