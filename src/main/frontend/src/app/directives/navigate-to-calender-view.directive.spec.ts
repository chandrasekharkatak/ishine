import { NavigateToCalenderViewDirective } from './navigate-to-calender-view.directive';

describe('NavigateToCalenderViewDirective', () => {
  it('should create an instance', () => {
    const directive = new NavigateToCalenderViewDirective();
    expect(directive).toBeTruthy();
  });  
});
  