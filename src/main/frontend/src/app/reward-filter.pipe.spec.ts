import { RewardFilterPipe } from './reward-filter.pipe';

describe('RewardFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new RewardFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
