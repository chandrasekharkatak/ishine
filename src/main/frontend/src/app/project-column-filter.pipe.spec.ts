import { ProjectColumnFilterPipe } from './project-column-filter.pipe';

describe('ProjectColumnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ProjectColumnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
